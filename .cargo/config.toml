# ROCm builds: HIP static libraries lack -fPIC, causing linker relocation
# errors with rust-lld. Using gcc as linker avoids -fuse-ld=lld injection.
# See ADR-016 prototype findings.
[target.x86_64-unknown-linux-gnu]
linker = "gcc"
