# Disable rust-lld for ROCm builds.
# ROCm static libraries lack -fPIC, which rust-lld requires.
# Setting linker to gcc directly avoids the -fuse-ld=lld injection.
[target.x86_64-unknown-linux-gnu]
linker = "gcc"
