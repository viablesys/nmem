[package]
name = "repo-map"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
git2 = "0.20"
regex = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
tempfile = "3"
